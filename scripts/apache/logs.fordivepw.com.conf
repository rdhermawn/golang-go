<VirtualHost *:80>
    ServerName logs.fordivepw.com

    RewriteEngine On
    RewriteRule ^/?(.*)$ https://logs.fordivepw.com/$1 [R=301,L]
</VirtualHost>

<IfModule mod_ssl.c>
    <VirtualHost *:443>
        ServerName logs.fordivepw.com

        SSLEngine on
        SSLCertificateFile /etc/letsencrypt/live/logs.fordivepw.com/fullchain.pem
        SSLCertificateKeyFile /etc/letsencrypt/live/logs.fordivepw.com/privkey.pem

        ProxyPreserveHost On
        ProxyRequests Off
        SSLProxyEngine On

        RequestHeader set X-Forwarded-Proto "https"
        RequestHeader set X-Forwarded-Port "443"

        ProxyPass /api/events/stream http://127.0.0.1:9090/api/events/stream retry=0 timeout=600 keepalive=On
        ProxyPassReverse /api/events/stream http://127.0.0.1:9090/api/events/stream

        ProxyPass / http://127.0.0.1:9090/ retry=0 timeout=60 keepalive=On
        ProxyPassReverse / http://127.0.0.1:9090/

        ErrorLog ${APACHE_LOG_DIR}/logs.fordivepw.com-error.log
        CustomLog ${APACHE_LOG_DIR}/logs.fordivepw.com-access.log combined
    </VirtualHost>
</IfModule>
